name: deployscp
on:
  push:
    branches: [ master ]

jobs:
  deploy_job:
    runs-on: ubuntu-latest
    name: deploy
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Deploy file
        uses: wlixcc/SFTP-Deploy-Action@v1.2.4
        with:
            username: demo6@demo6.conscor.com
            server: 88.99.164.225
            port: 21
            local_path: '*'
            remote_path: '/public_html/demo6.conscor.com/'
            sftp_only: true
            password: ${{ secrets.FTP_PASSWORD }}
