on: 
 push:
   branches: [ master ]
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v3
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: 3.110.32.0
        username: ec2-3-110-32-0.ap-south-1.compute.amazonaws.com
        password: ${{ secrets.FTP_PASSWORD }}
        protocol: ftps
        port: 21
